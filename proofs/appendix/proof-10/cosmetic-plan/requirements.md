# Requirements

## Requirements

1. R1 — The hero headline reads exactly "Train with the best in the Lowcountry" (spec line:
   "update the headline to ...").
2. R2 — The hero photo is replaced with the new team-shot image supplied by the user (spec line:
   "swap the hero photo for the new team shot").
3. R3 — The accent color is changed from orange to a deeper red everywhere the accent token is
   used (spec line: "change the accent color from orange to a deeper red").
4. R4 — Testimonial cards rotate automatically on a timer of a few seconds (~5s per A1) (spec
   line: "make the testimonial cards rotate automatically every few seconds").
5. R5 — Nothing else changes: same sections, same links, same forms, same page structure (spec
   line: "Nothing else changes — same sections, same links, same forms").
